id: clockwork-key

info:
  name: Clockwork Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "clockwork(.*?)[+0-9A-Za-z]{40}"