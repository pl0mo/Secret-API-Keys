id: heroku-platform-key

info:
  name: Heroku platform Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "HEROKU_API_KEY(.*?)[a-zA-Z0-9-_]{36}"