id: rails-secret-key-base

info:
  name: Rails Secret KeyBase
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "secret_key_base(.*?)[a-zA-Z0-9]{32}"