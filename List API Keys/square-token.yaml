id: square-token

info:
  name: Square Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)sq0atp[a-z0-9-_A-Z]{1,80}"