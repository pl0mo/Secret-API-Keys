id: npm-token

info:
  name: NPM Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)npm[a-z0-9A-Z-_]{1,50}"