id: zillow-key

info:
  name: Zillow Keys
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)X1-ZW[a-z0-9A-Z/-_]{1,80}"