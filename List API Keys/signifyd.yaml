id: signifyd-api-key

info:
  name: Signifyd API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)SIGNIFYD_APIKEY[a-z0-9A-Z-_=]{1,50}"