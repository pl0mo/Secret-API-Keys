id: anypoint-key

info:
  name: Anypoint Keys
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "<anypoint.platform.client_id>[0-9A-Za-z]{32}</anypoint.platform.clientid>"