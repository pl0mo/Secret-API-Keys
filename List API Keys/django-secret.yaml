id: django-secret-key

info:
  name: Django Secret Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "DJANGO_SECRET_KEY(.*?)[a-zA-Z0-9@-_#,!$&!%*]{51}"