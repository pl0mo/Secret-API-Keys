id: sumologic-key

info:
  name: Sumologic Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "sumologic.(accessId|accessKey)(.*?)[a-zA-Z0-9]{1,70}"