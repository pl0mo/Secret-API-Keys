id: stripe-webhook-secret

info:
  name: Stripe Webhook Secret 
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)whsec[a-z0-9-_A-Z]{1,80}"