id: private-dsa-key

info:
  name: DSA Private Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "\\s*(\\bBEGIN\\b).*(PRIVATE KEY\\b)\\s*"