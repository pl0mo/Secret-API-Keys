id: fcm-api-key

info:
  name: Firebase Cloud Management Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)apa91b(.*?)[a-zA-Z0-9]{134}"