id: clojars-deploy-token

info:
  name: Clojars Deploy Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "clojars_[+0-9A-Za-z]{60}"
          - "CLOJARS_[+0-9A-Za-z]{60}"