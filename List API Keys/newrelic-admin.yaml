id: newrelic-admin-key

info:
  name: NewRelic Admin Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "new_relic(.*?)[a-z0-9A-Z-_]{32}"
          - "(?i)NRAA-[a-z0-9]{27}"