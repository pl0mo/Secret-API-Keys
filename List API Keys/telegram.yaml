id: telegram-bot-token

info:
  name: Telegram Bot Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)[0-9a-zA-Z]{9,12}:aa[a-zA-Z0-9-_]{1,35}"