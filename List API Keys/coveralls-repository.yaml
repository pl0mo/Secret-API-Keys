id: coveralls-repository-token

info:
  name: Coveralls Repository Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "coveralls_repo_token=[a-zA-Z0-9]{33}"