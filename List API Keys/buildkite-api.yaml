id: buildkite-api-token

info:
  name: Buildkite API Token
  author: penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "BUILDKITE_ACCESS_TOKEN=(.*?)[a-z0-9A-Z-_]{40}"