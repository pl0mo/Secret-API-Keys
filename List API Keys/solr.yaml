id: solr-credential

info:
  name: SOLR Credentials
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "solr://[0-9A-Za-z+/:@.]{1,50}"