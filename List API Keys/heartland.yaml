id: heartland-api-key

info:
  name: Heartland Api Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "skapi_cert_[a-zA-Z0-9]{42}"