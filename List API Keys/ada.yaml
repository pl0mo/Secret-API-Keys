id: adafruit-api-key

info:
  name: Adafruit IO API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "aio_[0-9A-Za-z]{28}"