id: fastly-personal-token

info:
  name: Fastly Personal Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "fastly_apikey(.*?)[a-zA-Z0-9^:-]{32}"