id: pubnub-secret-key

info:
  name: Pubnub Secret Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "pnconfig.secret_key(.*?)[a-zA-Z0-9-_]{1,55}"