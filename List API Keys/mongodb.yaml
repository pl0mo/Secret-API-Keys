id: mongodb-credential

info:
  name: MongoDB credential
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)spring.data.mongodb\\.(url|port|username|password)=[a-zA-Z0-9-_=!@#$%^&*.//:]{1,50}"