id: moneywave-api-key

info:
  name: Moneywave API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "ts_(.*?)[A-Z0-9]{1,30}"