id: pusher-channel-key

info:
  name: Pusher Channels Keys
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)PUSHER_APP_(id|secret|key|cluster)[a-zA-Z0-9-_=]{1,20}"