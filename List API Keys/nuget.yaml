id: nuget-api-key

info:
  name: NuGet API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)oy2[a-z0-9A-Z-_]{1,50}"