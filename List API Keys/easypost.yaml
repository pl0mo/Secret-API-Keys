id: easypost-api-key

info:
  name: Easypost API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "EasyPost.apiKey(.*?)[0-9a-zA-Z-]{58}"
          - "new Easypost(.*?)[0-9a-zA-Z-]{58}"