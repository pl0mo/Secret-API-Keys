id: discord-webhook-url

info:
  name: Discord Webhook URL
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)(discordapp|discord)(.{0,18})[0-9/]{19}[a-zA-Z0-9-_]{68}"