id: circleci-personal-token

info:
  name: CircleCI Personal Token
  author: penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "circleci_token(.*?)[a-z0-9]{40}"