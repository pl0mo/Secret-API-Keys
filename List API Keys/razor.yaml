id: razor_pay-api-key

info:
  name: RazorPay API Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "rzp_live[a-z0-9A-Z-_]{15}"