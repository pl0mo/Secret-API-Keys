id: branch-key

info:
  name: Branch App Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)BRANCH_KEY(.*?)[a-zA-Z0-9=_-]{2,45}"