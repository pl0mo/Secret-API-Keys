id: square-credentials

info:
  name: Square Credentials
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)sq0idp[a-z0-9-_A-Z]{1,80}"
          - "(?i)sq0csp[a-z0-9-_A-Z]{1,80}"