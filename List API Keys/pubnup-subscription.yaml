id: pubnub-subscription-key

info:
  name: Pubnub Subcription Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "sub-c[a-z0-9-_]{1,45}"