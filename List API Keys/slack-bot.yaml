id: slack-bot-token

info:
  name: Slack Bot Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)xoxb[a-z0-9A-Z-_]{1,100}"