id: shippo-api-token

info:
  name: Shippo API Token
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?i)shippo_(test|live)[a-zA-Z0-9-_.]{1,100}"