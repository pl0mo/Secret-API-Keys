id: pulumi-access-token

info:
  name: Pubnub Publish Key
  author: Penyihir.exe
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "pul[a-z0-9-_]{1,45}"